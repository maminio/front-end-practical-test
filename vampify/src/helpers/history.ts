import { createBrowserHistory } from 'history';
/**
 * create an instance from history so we can navigate user from every files to a specific route
 * this instance is passed as history argument of route located in App.tsx
 */
export const history = createBrowserHistory();
